-- ============================================================================
-- Phase 1 · 清空原型 mock rule_versions（受保护的一次性清空，已迁移至 seed 路径）
--
-- 重要变更（2026-09-01）：
--   原实现「每启动 DROP + CREATE rule_versions」会破坏跨重启持久化
--   （用户自定义规则 R900/R901 重启即丢失），且无条件清掉所有行违反
--   「重启不丢」原则。已改为：
--
--   ① 本迁移保留为【无操作占位】（rule_versions 已由 schema.js DDL + 不可变触发器建好）；
--   ② 真正的「一次性受保护清空 mock」下沉到 server/src/index.js 的
--      createService 启动路径中的 clearLegacyMockRules()：
--        - 仅当 rule_versions 中【存在非 V9.7 残留行】且【尚无 V9.7 规则】时，
--          才临时禁用不可变触发器 → DELETE 残留行 → 重建触发器；
--        - 已迁移（含 V9.7 标记）或空表：跳过，保护用户自定义规则跨重启存活；
--   ③ V9.7 真规则由 rules.seed() 幂等重灌（INSERT OR IGNORE）。
--
-- 因此本文件不应再包含任何 DDL/DML，避免 runMigrations 每启动重复执行。
-- 维护：若需重新触发 mock 清空，删除对应 DB 后重启服务即可（seed 路径会重灌 V9.7）。
-- ============================================================================
