# 系统功能全景审视 · 2026-09-03

> 目的：按 PC 端（前端原型 prototype-1.0.0）每个功能模块 + 后端每个模块，说明**作用 / 数据源 / 当前状态**。
> 状态口径：`✅ 跑通`（真实数据端到端 + 测试覆盖）· `⚠️ 部分`（有实现但空转/占位/未接线）· `❌ 未开发`。
> 事实来源：代码探查 + 真机验证（DB 161 场 / 官方端点 200 / 回归 416 过 2 跳过）。

---

## 一、总体分层

```
数据层 data/ ──→ 特征层 features/ ──→ 规则/引擎层 rules/+engine/v97/ ──→ 融合层 fusion/ ──→ 发布层 publish/
     │                                                                        ↑
     └── 工程治理层：db/(落库) · backtest/(回测) · promote/(转正) · ai/(挖掘) · vault/(凭证审计) · gateway/(鉴权限流) · http/(端点)
```

---

## 二、PC 端 13 个功能模块

| # | 页面 | 作用 | 数据源 | 状态 |
|---|---|---|---|---|
| 1 | **首页** | 竞彩"今日可买"列表、场次关联人工盘赔、开启分析、进入三栏分析 | 官方赔率端点（真实，当天 20 场）+ 合并池 + `/api/merged/analysis` v97 | ✅ **跑通**（今天这场 2041244 已 `merged=true` 并显示 E14/S25 真规则结论） |
| 2 | **预测链** pipeline | 可视化"数据→特征→规则→融合→结论"链路 | 全 Mock（29 处 mock 痕迹），不调 API | ❌ **未接真链**（V9.7 后旧链已空转） |
| 3 | **规则治理** governance | 8 态生命周期、回测监督、推进/重置、审计 | 本地 mock 状态 + backtest.js 种子指标 | ⚠️ **部分**（生命周期/门禁已加：validated/active 需回测达标 + 拦截审计；但指标本身是种子值，未消费后端 v97_real） |
| 4 | **回测** backtest | 规则回测指标与阈值展示 | 本地 mock 指标 | ⚠️ **部分**（后端 `/api/backtest/:id` 已有 **v97_real 真实回测**，页面尚未消费） |
| 5 | **数据接入** ingest | 数据源可观测：官方赛程/赔率、本地盘赔、合并池状态 | 4 个 `/api/sources/*` 端点（真实） | ✅ **跑通** |
| 6 | **历史赛事** historical | 历史场次列表 + 单场分析（含合并池分析） | `/api/sources/merged`、`merged/analysis`（真实 161 场） | ✅ **跑通** |
| 7 | **特征引擎** feature | 特征目录/家族展示 | 本地 FEATURE_CATALOG（23 字段注册表） | ⚠️ **展示为静态目录**（后端特征真实计算 + point-in-time 已在分析响应里带出，页面未接后端） |
| 8 | **DSL 引擎** dsl | 条件树/算子求值演示 | Mock 规则 + 本地 DSL | ❌ **空转**（V9.7 真规则 atoms 不喂 DSL；`convert/` 文字→DSL 已清空为 no-op 兼容壳） |
| 9 | **后端接入** api | mock/http 双适配切换、端点连通性冒烟 | api-client + 真实后端 | ✅ **跑通** |
| 10 | **历史记录** history | 保存的分析快照/复盘列表 | localStorage（保存的是**旧 DSL 链**分析结果） | ⚠️ **内容需迁移**（旧链空 → 记录价值有限，应改存 v97 结论） |
| 11 | **规则库** rules | 规则检索/命中预览/编辑 | 前端 mock RULES（R001–R016）；真实 88 条在后端 | ⚠️ **双轨**（真实规则经 `/api/rules` 可取；页面编辑走 mock，已改不可变版本台账） |
| 12 | **AI 引擎** ai | AI 候选挖掘 + 人在环审核（采纳/驳回） | 后端 `/api/ai/candidates`（**stub provider 假候选**）+ review→proposed | ⚠️ **部分**（流程与隔离闭环：untrusted→proposed→回测门禁；**无真实模型**） |
| 13 | **设置** settings | 模式/阈值/置信度度量配置 | 本地配置 | ⚠️ **部分**（edge/ROI 开关为占位，当前仍命中率加权） |

---

## 三、后端 20+ 模块

### ✅ 已跑通（真实数据 + 测试覆盖）
| 模块 | 作用 | 证据 |
|---|---|---|
| **data/** | 接入（官方赛程/赔率适配器）、人工盘赔解析、双源合并、**期号锚定** | 161 场盘赔入池；官方端点 200/20 场；`merged=true`（2041244）；`align_via` 留痕 |
| **db/** | SQLite 落库、12+3 张 qd_* 表、不可变触发器、幂等 reconcile、派生版本化 | 161/161 场带赛果；imported/superseded 幂等 |
| **rules/** | 规则存储、8 态状态机、规则级锁、**v97loader（88 条真规则 + 启动门禁）** | 88 规则全量载入，门禁拦截不一致版本 |
| **engine/v97/** | 字段信封（三态）、atom 求值、effects→维度 | 12/208 字段可求值（11 个 100%）；R01/R13/S25 完全可求值 |
| **backtest/** | 5 项准入 + 6 项指标 + **v97_real 真实回测（161 场）** | R01 hit12 / R13 hit19 / S25 hit87（探针 52/87=59.8%） |
| **features/** | 四族 + 欧指 + 必发特征、point-in-time 防泄漏、23 字段注册表 | 分析响应带真实特征快照 |
| **http/** | 12 个业务端点 + 统一响应壳 + CORS | 全量回归 + http-integration PASS |
| **gateway/** | 多 Key/撤销(401/403)、常量时间比较、限流(内存/Redis)、TLS 终止 | 专项测试 + deploy-smoke（HTTPS+Key+Redis 同进程） |
| **vault/** | 凭证隔离（域×角色矩阵）、审计 append-only | AI 角色禁取 data_source |
| **cache/queue/lock/** | 内存实现 + ioredis 实现，未配置优雅降级 | Redis RESP 集成 + 真实 Redis e2e |

### ⚠️ 部分（有实现但空转 / 占位 / 未接线）
| 模块 | 作用 | 缺口 |
|---|---|---|
| **fusion/** | 三路融合（规则/统计/异常）+ 权重 + 置信度门 + 信任隔离——**已实现且接线**（engine/prediction + worker） | **V9.7 真规则结果未接入**（旧 DSL 链无输入 → 空转）；edge/ROI 未实现 |
| **worker/** | 检索 Worker：point-in-time 检索、冲突检测、仲裁 | 输入来自旧链，同上空转 |
| **dsl/** | 11 算子 + 编译期校验 + 推理链 | 不消费 V9.7 atoms；`custom` 算子 13 处未实现 |
| **convert/** | 文字规则→DSL 历史兼容 | catalog 已清空，保留 no-op 入口 |
| **ai/** | 多 provider 适配 + 挖掘 + 单场解读 +  containment 隔离 + review 转正 | provider 为 **stub**（假候选）；无真实模型 |
| **promote/** | 回测转正（draft→…→active）+ metrics 门禁 | 门禁完备（样本≥30/命中≥55%/ROI/回撤/稳定/联赛覆盖），但**无真实 eligible 样本喂入、无 HTTP 端点** |
| **engine/prediction** | 预测输出 + 证据冻结 | 只 `freezeEvidence`，**未调用 publish 落库** |

### ❌ 未开发 / 未接线（能力缺位）
| 项 | 说明 |
|---|---|
| **publish（预测发布/赛果回填）** | 8 个文件实现完整（不可变预测、幂等、赛果回填、证据锁定），但**无 HTTP 端点且未被引擎调用** → 前端无法保存预测/回填赛果 |
| **196 个字段** | 多数需**非盘口输入**（伤停/身价/战意/赛制细节），`value_gap_ratio`=身价差倍数已确认不可从盘赔推导 |
| **custom 算子（13 处）** | R05/E02 等关键规则依赖，语义需按规则逐条定义 |
| **统计模型（GBDT/XGBoost）** | 融合层"统计模型"一路仍为 placeholder，未训练 |
| **置信度 edge/ROI** | 设置页开关占位，当前命中率加权（让球盘基线≈50%，不足以度量优势） |
| **规则→赛果语义映射** | 方向型规则转正后进统一仲裁的策略待定（见 fusion-decision-layer.md） |
| **生产闭环** | 长期运行观测/告警/容量压测/线上部署、前端生产化 |

---

## 四、当前真正跑通的三条价值链

1. **盘赔数据链**：OCR `盘口数据.md` → reconcile（幂等版本化）→ DB 161 场（全带赛果）→ 合并池 → 单场分析
2. **官方锚定链**：`webapi.sporttery.cn` 赔率端点 200（当天 20 场）→ 锚源 `odds` → 期号确定性对齐（`merged=true`、官方 ID 接管）
3. **真规则求值链**：88 条 V9.7 → 12 字段信封 → 3 条规则完全可求值 → 真实回测 161 场（S25 探针 59.8%）→ 前端"V9.7 真规则求值"区块渲染

> 关键判断：**数据侧与规则求值侧已真实可用；"预测/发布/融合"这一段仍是空转的旧链**——因为 V9.7 结果没有接进 fusion，也没有落库/回填端点。

---

## 五、建议的开发优先级

| 优先级 | 事项 | 价值 |
|---|---|---|
| **P0** | 把 **v97 结果接入 fusion**（v97 dimensions → 融合输入；gate/weight/total_goals_signal 分流） | 让"预测链/融合层"从空转变成真链，方向结论可出 |
| **P0** | **publish 端点化**（保存预测 / 赛果回填幂等）+ 前端接"保存复盘" | 补齐预测→回填闭环，历史记录页内容才有价值 |
| **P1** | S25 转正试点（87 场 59.8% 证据 → promote validated；总进球方向映射） | 规则自我生长第一个完整闭环 |
| **P1** | 前端页面消费真实接口：回测页接 v97_real、特征页接后端特征、规则页接 88 条 | 消除双轨，去掉 mock 展示 |
| **P2** | 第二批字段（需先确认手册定义/新数据源）；custom 算子按规则实现 | 提升规则点亮率 |
| **P3** | edge/ROI 置信度、统计模型、生产部署闭环 | 长期 |
